<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice {{invoiceNumber}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Arial', sans-serif;
            color: #2c3e50;
            line-height: 1.6;
            padding: 40px;
        }
        .invoice-container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
        }
        .header {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            max-width: 180px;
            max-height: 70px;
        }
        .invoice-title h1 {
            font-size: 48px;
            font-weight: 300;
        }
        .invoice-number {
            font-size: 16px;
            opacity: 0.9;
        }
        .content {
            padding: 40px;
        }
        .info-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            margin-bottom: 40px;
            padding: 30px;
            background: #f8f9fa;
            border-radius: 5px;
        }
        .info-block h3 {
            font-size: 12px;
            text-transform: uppercase;
            color: #2c3e50;
            margin-bottom: 12px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .info-block p {
            margin: 4px 0;
            font-size: 14px;
            color: #555;
        }
        .info-block strong {
            color: #2c3e50;
            font-weight: 600;
        }
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }
        .items-table thead {
            background: #2c3e50;
            color: white;
        }
        .items-table th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .items-table td {
            padding: 15px;
            border-bottom: 1px solid #ecf0f1;
            font-size: 14px;
        }
        .items-table tbody tr:last-child td {
            border-bottom: 2px solid #2c3e50;
        }
        .text-right {
            text-align: right;
        }
        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-top: 30px;
        }
        .totals {
            width: 400px;
            background: #f8f9fa;
            padding: 25px;
            border-radius: 5px;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            font-size: 15px;
            color: #555;
        }
        .total-row.subtotal {
            border-top: 1px solid #dee2e6;
            padding-top: 15px;
        }
        .total-row.final {
            background: #2c3e50;
            color: white;
            padding: 20px;
            margin: 15px -25px -25px;
            border-radius: 0 0 5px 5px;
            font-size: 22px;
            font-weight: 600;
        }
        .notes-section {
            margin-top: 40px;
            padding: 25px;
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            border-radius: 5px;
        }
        .notes-section h4 {
            color: #856404;
            margin-bottom: 10px;
            font-size: 14px;
            text-transform: uppercase;
        }
        .notes-section p {
            color: #856404;
            font-size: 14px;
        }
        .payment-info {
            margin-top: 30px;
            padding: 25px;
            background: #d1ecf1;
            border-left: 4px solid #17a2b8;
            border-radius: 5px;
        }
        .payment-info h4 {
            color: #0c5460;
            margin-bottom: 15px;
            font-size: 14px;
            text-transform: uppercase;
        }
        .payment-info p {
            color: #0c5460;
            font-size: 13px;
            margin: 8px 0;
        }
        .footer {
            margin-top: 50px;
            padding-top: 30px;
            border-top: 2px solid #ecf0f1;
            text-align: center;
        }
        .footer p {
            color: #95a5a6;
            font-size: 13px;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            <div>
                {{#if companyInfo.logo}}
                <img src="{{companyInfo.logo}}" alt="{{companyInfo.name}}" class="logo">
                {{else}}
                <h2>{{companyInfo.name}}</h2>
                {{/if}}
            </div>
            <div class="invoice-title">
                <h1>INVOICE</h1>
                <div class="invoice-number">#{{invoiceNumber}}</div>
            </div>
        </div>

        <div class="content">
            <div class="info-grid">
                <div class="info-block">
                    <h3>Billed From</h3>
                    <p><strong>{{companyInfo.name}}</strong></p>
                    {{#if companyInfo.address}}<p>{{companyInfo.address}}</p>{{/if}}
                    {{#if companyInfo.city}}<p>{{companyInfo.city}}, {{companyInfo.state}} {{companyInfo.zipCode}}</p>{{/if}}
                    {{#if companyInfo.country}}<p>{{companyInfo.country}}</p>{{/if}}
                    {{#if companyInfo.email}}<p>{{companyInfo.email}}</p>{{/if}}
                    {{#if companyInfo.phone}}<p>{{companyInfo.phone}}</p>{{/if}}
                    {{#if companyInfo.taxId}}<p>Tax ID: {{companyInfo.taxId}}</p>{{/if}}
                </div>

                <div class="info-block">
                    <h3>Billed To</h3>
                    <p><strong>{{clientInfo.name}}</strong></p>
                    {{#if clientInfo.address}}<p>{{clientInfo.address}}</p>{{/if}}
                    {{#if clientInfo.city}}<p>{{clientInfo.city}}, {{clientInfo.state}} {{clientInfo.zipCode}}</p>{{/if}}
                    {{#if clientInfo.country}}<p>{{clientInfo.country}}</p>{{/if}}
                    {{#if clientInfo.email}}<p>{{clientInfo.email}}</p>{{/if}}
                    {{#if clientInfo.phone}}<p>{{clientInfo.phone}}</p>{{/if}}
                </div>

                <div class="info-block">
                    <h3>Invoice Information</h3>
                    <p><strong>Invoice Date:</strong> {{invoiceDate}}</p>
                    <p><strong>Due Date:</strong> {{dueDate}}</p>
                    {{#if purchaseOrder}}<p><strong>PO Number:</strong> {{purchaseOrder}}</p>{{/if}}
                    <p><strong>Currency:</strong> {{currency}}</p>
                </div>
            </div>

            {{#if customFields}}
            <div style="margin-bottom: 30px; padding: 20px; background: #f8f9fa; border-radius: 5px;">
                {{#each customFields}}
                <p style="margin: 8px 0;"><strong>{{label}}:</strong> {{value}}</p>
                {{/each}}
            </div>
            {{/if}}

            <table class="items-table">
                <thead>
                    <tr>
                        <th style="width: 45%;">Description</th>
                        <th>Quantity</th>
                        <th>Unit</th>
                        <th class="text-right">Unit Price</th>
                        <th class="text-right">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each items}}
                    <tr>
                        <td>{{description}}</td>
                        <td>{{quantity}}</td>
                        <td>{{unit}}</td>
                        <td class="text-right">{{formatCurrency unitPrice ../currency}}</td>
                        <td class="text-right"><strong>{{formatCurrency lineTotal ../currency}}</strong></td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

            <div class="totals-section">
                <div class="totals">
                    <div class="total-row subtotal">
                        <span>Subtotal:</span>
                        <span>{{formatCurrency subtotal currency}}</span>
                    </div>
                    {{#if discount}}
                    <div class="total-row">
                        <span>Discount ({{discountRate}}%):</span>
                        <span>-{{formatCurrency discount currency}}</span>
                    </div>
                    {{/if}}
                    {{#if tax}}
                    <div class="total-row">
                        <span>Tax ({{taxRate}}%):</span>
                        <span>{{formatCurrency tax currency}}</span>
                    </div>
                    {{/if}}
                    <div class="total-row final">
                        <span>TOTAL DUE:</span>
                        <span>{{formatCurrency total currency}}</span>
                    </div>
                </div>
            </div>

            {{#if notes}}
            <div class="notes-section">
                <h4>Notes & Terms</h4>
                <p>{{notes}}</p>
            </div>
            {{/if}}

            {{#if paymentTerms}}
            <div class="payment-info">
                <h4>Payment Information</h4>
                {{#if paymentTerms.bankName}}<p><strong>Bank Name:</strong> {{paymentTerms.bankName}}</p>{{/if}}
                {{#if paymentTerms.accountNumber}}<p><strong>Account Number:</strong> {{paymentTerms.accountNumber}}</p>{{/if}}
                {{#if paymentTerms.routingNumber}}<p><strong>Routing Number:</strong> {{paymentTerms.routingNumber}}</p>{{/if}}
                {{#if paymentTerms.paymentMethods}}
                <p><strong>Accepted Payment Methods:</strong> {{join paymentTerms.paymentMethods ", "}}</p>
                {{/if}}
            </div>
            {{/if}}

            <div class="footer">
                <p><strong>Thank you for your business!</strong></p>
                {{#if companyInfo.website}}<p>{{companyInfo.website}}</p>{{/if}}
                {{#if companyInfo.email}}<p>{{companyInfo.email}}</p>{{/if}}
            </div>
        </div>
    </div>
</body>
</html>
