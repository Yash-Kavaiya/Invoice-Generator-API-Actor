<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice {{invoiceNumber}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Georgia, 'Times New Roman', serif;
            color: #000;
            line-height: 1.6;
            padding: 40px;
        }
        .invoice-container {
            max-width: 800px;
            margin: 0 auto;
            border: 2px solid #000;
            padding: 40px;
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 2px solid #000;
        }
        .header h1 {
            font-size: 36px;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .logo {
            max-width: 150px;
            margin-bottom: 20px;
        }
        .invoice-number {
            font-size: 16px;
            font-style: italic;
        }
        .info-section {
            display: flex;
            justify-content: space-between;
            margin-bottom: 40px;
        }
        .info-block {
            flex: 1;
        }
        .info-block h3 {
            font-size: 14px;
            text-decoration: underline;
            margin-bottom: 10px;
        }
        .info-block p {
            margin: 3px 0;
            font-size: 13px;
        }
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            border: 1px solid #000;
        }
        .items-table th,
        .items-table td {
            padding: 12px;
            border: 1px solid #000;
            font-size: 13px;
        }
        .items-table th {
            background: #f0f0f0;
            font-weight: bold;
            text-align: left;
        }
        .text-right {
            text-align: right;
        }
        .totals {
            margin-left: auto;
            width: 300px;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 8px;
            font-size: 14px;
            border-bottom: 1px solid #ccc;
        }
        .total-row.final {
            border: 2px solid #000;
            font-weight: bold;
            font-size: 18px;
            margin-top: 10px;
        }
        .notes {
            margin-top: 40px;
            padding: 20px;
            border: 1px solid #000;
        }
        .notes h4 {
            margin-bottom: 10px;
            text-decoration: underline;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-style: italic;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            {{#if companyInfo.logo}}
            <img src="{{companyInfo.logo}}" alt="{{companyInfo.name}}" class="logo">
            {{/if}}
            <h1>INVOICE</h1>
            <div class="invoice-number">No. {{invoiceNumber}}</div>
        </div>

        <div class="info-section">
            <div class="info-block">
                <h3>From:</h3>
                <p><strong>{{companyInfo.name}}</strong></p>
                {{#if companyInfo.address}}<p>{{companyInfo.address}}</p>{{/if}}
                {{#if companyInfo.city}}<p>{{companyInfo.city}}, {{companyInfo.state}} {{companyInfo.zipCode}}</p>{{/if}}
                {{#if companyInfo.email}}<p>{{companyInfo.email}}</p>{{/if}}
                {{#if companyInfo.phone}}<p>{{companyInfo.phone}}</p>{{/if}}
            </div>

            <div class="info-block">
                <h3>To:</h3>
                <p><strong>{{clientInfo.name}}</strong></p>
                {{#if clientInfo.address}}<p>{{clientInfo.address}}</p>{{/if}}
                {{#if clientInfo.city}}<p>{{clientInfo.city}}, {{clientInfo.state}} {{clientInfo.zipCode}}</p>{{/if}}
                {{#if clientInfo.email}}<p>{{clientInfo.email}}</p>{{/if}}
            </div>

            <div class="info-block">
                <h3>Details:</h3>
                <p>Date: {{invoiceDate}}</p>
                <p>Due: {{dueDate}}</p>
                <p>Currency: {{currency}}</p>
            </div>
        </div>

        <table class="items-table">
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Qty</th>
                    <th>Unit</th>
                    <th class="text-right">Rate</th>
                    <th class="text-right">Amount</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                <tr>
                    <td>{{description}}</td>
                    <td>{{quantity}}</td>
                    <td>{{unit}}</td>
                    <td class="text-right">{{formatCurrency unitPrice ../currency}}</td>
                    <td class="text-right">{{formatCurrency lineTotal ../currency}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="totals">
            <div class="total-row">
                <span>Subtotal:</span>
                <span>{{formatCurrency subtotal currency}}</span>
            </div>
            {{#if discount}}
            <div class="total-row">
                <span>Discount:</span>
                <span>-{{formatCurrency discount currency}}</span>
            </div>
            {{/if}}
            {{#if tax}}
            <div class="total-row">
                <span>Tax:</span>
                <span>{{formatCurrency tax currency}}</span>
            </div>
            {{/if}}
            <div class="total-row final">
                <span>TOTAL DUE:</span>
                <span>{{formatCurrency total currency}}</span>
            </div>
        </div>

        {{#if notes}}
        <div class="notes">
            <h4>Terms & Notes</h4>
            <p>{{notes}}</p>
        </div>
        {{/if}}

        <div class="footer">
            <p>Thank you for your patronage</p>
        </div>
    </div>
</body>
</html>
